1) Python Modules(requirements.txt)
-I used 2 python modules.
	beautifulsoup4==4.1.3
	mechanize==0.2.5
-In requirements.txt, there are modules which I used for this program.
-You should install those modules first.
-Open terminal and enter commands like this:

$cd <the path of this directory>
$sudo pip install -r requirements.txt

2) You can run bensbargains-scraper.py file on console.This is just a program file.

$cd <the path of this directory>
$python ./bensbargains-scraper.py

3) after scrapping done, you can see "bensbargains.json" file in the directory.
And you can see "successfully completed!!!" message on terminal.